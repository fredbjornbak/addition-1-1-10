
import React from 'react';
import CompactVisualWorkspace from '../components/CompactVisualWorkspace';

const VisualAddition = () => {
  return (
    <div className="min-h-screen bg-grade-white flex items-center justify-center">
      <CompactVisualWorkspace />
    </div>
  );
};

export default VisualAddition;
